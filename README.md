# Antenna Rotator Server
Go based server that allows remote control of antenna rotators 

# Notes
- gousb requires the libusb C library installed for build and runtime (https://github.com/libusb/libusb/wiki/Windows)